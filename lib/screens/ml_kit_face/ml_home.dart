import 'package:flutter/material.dart';

class MlHome extends StatelessWidget {
  const MlHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('face loged success'),
      ),
    );
  }
}
